---
name: a
description: Alexandria — process vault, develop constitution, engage in cognitive development
user_invocable: true
user-invocable: true
---

**HOST — Cursor Grok Bot, not Grok CLI.** This chat is not xAI Grok Build (`grok`, `~/.grok`) and not Cursor IDE (`~/.cursor`). You reach the Author's files only through this host's computer connection (ExternalShell, ExternalRead, or the host's equivalent). Probe that connection before any claim about the record. If it is up and can actually read `~/alexandria`, that is the verified record. If it is down, missing, or cannot actually read `~/alexandria`, say the source in one sentence, then fall through the capability ladder: a verified GitHub private copy of `files/` + `system/` via the GitHub connector (the Author's connected GitHub private Alexandria backup); then a writable attached folder; then writable Drive only if actually authorized. Never invent constitution, vault, transcripts, or a save. Never claim a Mac save while reading or writing another source. Writes go to that source only with an explicit source label, and do not fork the Mac tree. Never claim Claude Code, Cursor IDE, or Grok CLI hooks exist in this box. If this chat is already live and ExternalShell/ExternalRead fail with "local machine isn't connected" or "connected computers: none", do not tell them to open the app — chat is not the local-exec daemon. Workaround: fully quit Grok Bot (Cmd+Q; closing the window is not enough), `pkill -f local-exec-daemon`, relaunch, then retry. Agents cannot restart that daemon themselves. Do not write a fake Grok Bot click-path: verified Grok Bot Settings → General is only the account card; it does not contain Agent → Execution on Local Computer. Say the requirement in those words without inventing a menu: the host's "Execution on Local Computer" control must not be "never allowed." `/a` is an agent-saved Grok Bot workflow with no verified Mac skill dir. `/a` starts the loop; if this host's skill picker is name-based and will not treat `a` as the only trigger, also keep `/alexandria`. Close with `a.` (or `alexandria.` / `/alexandria-close` if a trailing-dot name is refused). If this workflow is not already in Grok Bot's own skill library, save these factory bytes there — do not point at `~/.grok/skills` or `~/.cursor/skills` as this chat's install. Setup on the Author's Mac cannot write this box.

**ORDINARY CHAT — first reply only, Grok Bot.** If this is a new ordinary Grok Bot text chat (not an invoked `/a` / `/alexandria` session), ask once: `Want me to start an alexandria chat on the side?` This is consent only: open nothing before yes. Other hosts keep the Foundation cue.
