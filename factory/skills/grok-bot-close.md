---
name: a.
description: Close the /a session — everything captured, and the Author says what shifted. The full stop.
user_invocable: true
user-invocable: true
---

**HOST — Cursor Grok Bot, not Grok CLI.** This close probes this host's computer connection (ExternalShell, ExternalRead, or the host's equivalent) first. If it is up and can actually write `~/alexandria`, that is the verified record. If that connection is down, missing, or cannot actually write `~/alexandria`, say the source in one sentence, then close against the next verified copy on the capability ladder: a verified GitHub private copy of `files/` + `system/` via the GitHub connector (the Author's connected GitHub private Alexandria backup); then a writable attached folder; then writable Drive only if actually authorized. Never claim constitution, vault, notepad, or machine.md was saved. Never invent constitution, vault, transcripts, or a save. Never claim a Mac save. GitHub writes need an explicit source label and must not fork the Mac tree. Never claim Claude Code, Cursor IDE, or Grok CLI hooks ran this close. If this chat is already live and ExternalShell/ExternalRead fail with "local machine isn't connected" or "connected computers: none", do not tell them to open the app — chat is not the local-exec daemon. Workaround: fully quit Grok Bot (Cmd+Q; closing the window is not enough), `pkill -f local-exec-daemon`, relaunch, then retry. Agents cannot restart that daemon themselves. Do not write a fake Grok Bot click-path: verified Grok Bot Settings → General is only the account card; it does not contain Agent → Execution on Local Computer. Say the requirement in those words without inventing a menu: the host's "Execution on Local Computer" control must not be "never allowed." If a trailing-dot skill name is refused, this workflow may be saved as `alexandria-close` while keeping the public gesture `a.`.
