---
name: a.
description: Close the /a session — everything captured, and the Author says what shifted. The full stop.
user_invocable: true
user-invocable: true
---

**HOST — Cursor Grok Bot, not Grok CLI.** This close probes the computer connection first. If it can actually write `~/alexandria`, that is the verified record. If not, say the source, then close against the next verified copy this session actually used: a connected Airlock recognized by `CONTEXT.manifest` plus `context/` and `inbox/`; then a writable Alexandria folder the Author deliberately attached; then authorized Drive; then this chat. Archive or save only to the source this session used. In an Airlock, return work only through `inbox/` or the assigned capture-issue label and say that it awaits review; never claim it changed canon. Never ask for or use the Author's general GitHub login, full sovereign repo, or Apple login as a fallback. If Mac is down, do not pretend `a.` wrote the vault. Never invent. Never claim a Mac save. GitHub writes need an explicit source label and the last commit, and must not fork a Mac tree we cannot see. Never claim Claude Code, Cursor IDE, or Grok CLI hooks ran this close. If this chat is already live and ExternalShell/ExternalRead fail with "local machine isn't connected" or "connected computers: none", do not tell them to open the app — chat is not the local-exec daemon. Workaround: fully quit Grok Bot (Cmd+Q; closing the window is not enough), `pkill -f local-exec-daemon`, relaunch, then retry. Agents cannot restart that daemon themselves. Do not write a fake Grok Bot click-path: verified Grok Bot Settings → General is only the account card; it does not contain Agent → Execution on Local Computer. Say the requirement in those words without inventing a menu: the host's "Execution on Local Computer" control must not be "never allowed." If a trailing-dot skill name is refused, this workflow may be saved as `alexandria-close` while keeping the public gesture `a.`.

**This closes the ACTIVE (/a) session.** Structurally separate from any work-session `/close` — different engine, different job; never conflate them. Three gestures, one skill: `/a.`, the bare message `a.`, or the plain full-name alias `alexandria.` (seeing any of them means invoking this skill first, never running the close from memory: long sessions compact context, and a close run from a compressed recollection is where value evaporates silently). There is no duplicate `alexandria.` close skill.

Two jobs, one bar: **all the value captured, all the value internalised — because it's all internal.**

**1. Captured — the files get everything (silent, first).** Follow Foundation's local close. If `~/alexandria/system/canon/methodology.md` exists, also run its Session Close method; its absence is a valid Author choice, not a failure. Write constitution signal, machine.md calibration, notepad carry-forward, and feedback to their own homes in the Author's words. Run Root Stewardship before the reflection: fold any load-bearing candidate, cumulative drift and material model influence from this session into the pending packet in `files/works/root-packets/`, with only a pointer in marginalia; complete an independent review only if the Author opened this qualifying model. Never call another model or send it a packet without a fresh yes for that exact packet and destination. Then run the Author's own close protocol if one exists (`~/alexandria/files/core/machine.md § Close protocol`). Resolve every path through the substrate map when present. Verify each write landed on the verified source you named (read-back or git) — nothing sweeps what slips; an unverified write is a lost write. Never claim a Mac save while writing another source.

**2. Internalised — THEY say what shifted, never you (design law, 2026-07-27).** Only the Author can truly say what moved — it is about their mind, not about the AI and what it thinks happened. After the silent capture, open the space:

- **Three or four threads, hard cap** — the ones with the most movement in them, however many the session actually touched. Making the cut is your job and it happens silently: the rest are dropped, never listed, never apologised for. **A complete list is a wall, and a wall gets skipped** (Author #1, 2026-08-03: *"i saw mine once and it was just too daunting and required lots of activation energy so i just skipped it"*). The whole block reads in about five seconds or it has failed. **The cap is deliberately hard, not a soft default** — the failure mode is an Engine reasoning its way to a fifth and sixth line because every thread felt important, which is precisely how the wall got built. Five seconds is the bar; the count is how you hit it.
- **Each line is: topic → one candidate takeaway, six words or fewer, quoted from THEM.** Never a conclusion in your own voice. The prior law was pointers-never-conclusions — because your verdict makes them confirm your read instead of finding theirs, and that risk is real and unchanged. What answers it is authorship, not blankness: their own sentence handed back has them reacting to their own thinking, a far smaller lift than generating from a bare topic. If a thread threw off no line of theirs worth quoting, give the topic alone — never manufacture a takeaway to fill the slot. Accepted cost: some nights they answer "yeah, that" and the real shift stays unsaid; that is the price of a block they actually read.
- Then the ask — "before WE close", never "before it closes" (the session is shared; the close is too) — and it is ONE line, calm enough to read every single day without wearing out: *what do you think now that you didn't when we started?* The why (their words stick; unanchored words keep the mirror accurate) is for YOU — never recited at close. Then give them room. Let them ramble. Don't interrupt, don't complete their sentences, don't grade the answer.

The ask, canonical shape (topics vary, form holds; the time-word fits the session):

```
before we close — tonight:

· <topic> → <their own line, six words max>
· <topic> → <their own line, six words max>
· <topic> → <their own line, six words max>

what do you think now that you didn't when we started?
```

- **The ramble is pure gold, twice over.** Saying it themselves IS the internalisation — what a person articulates, they keep; no artifact substitutes for their own sentence. And it is ground truth of the mind as it now is — the highest-grade calibration data the mirror ever gets.
- **File it like gold:** the verbatim reflection → the vault (dated, marked as a close-reflection); each shift **in their exact phrasing** → the delta ledger (`files/works/deltas.md`: their words are the headline — never rewrite them); constitution where a position crystallised; and any gap between their account and what you thought happened → machine.md as mirror calibration — that divergence is signal about you, not them.
- "Nothing really shifted" is a full answer. If they walk away without answering, close quietly — the capture already ran; the reflection is invited, never gated.

Then remove this session's line from `~/alexandria/system/.active_a_sessions` (the tab's footer returns to the launch teaser) and sign off, nothing after it:

a.
