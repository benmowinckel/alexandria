<!-- alexandria:start -->
## Alexandria

Alexandria is the Author's own file-based method at `~/alexandria`, not a separate assistant. Preserve every existing workflow and instruction. The session hooks load the current Author context when trusted; the file-only floor is always available: read `~/alexandria/system/canon/foundation.md` and the relevant files under `~/alexandria/files/` when personal context matters.

When the Author types `a` or `/a`, invoke the installed Alexandria start skill. When they type `a.` or `/a.`, invoke the separate close skill before doing anything else. During ordinary work, keep Alexandria passive. Put Alexandria product feedback in `~/alexandria/system/.session_feedback`; never replace platform memory or the Author's pre-existing instructions.
<!-- alexandria:end -->
