<!-- alexandria:start -->
## Alexandria

Alexandria is the Author's own file-based method at `~/alexandria`, not a separate assistant. Preserve every existing workflow and instruction. The session hooks load the current Author context when trusted; the file-only floor is always available: read `~/alexandria/system/canon/foundation.md` and the relevant files under `~/alexandria/files/` when personal context matters.

When the Author explicitly invokes an installed Alexandria start or close skill, follow that skill. Do not reinterpret a name owned by a pre-existing foreign skill; setup preserves collisions rather than taking them over. During ordinary work, keep Alexandria passive. If no native statusline is visibly carrying the cue, finish only the first assistant reply in a chat with the exact one-line output of `bash ~/.local/share/alexandria/scripts/statusline.sh footer-codex`. On later replies, show no generic footer; only when an Author file materially improved the answer, end with one combined italic line naming the input and the action, for example `*drew from: Taste.md · start $a in a new chat*`. During an active Alexandria session use `→ close with a. when done` instead. Never render a bare `$a`, open a chat, or start a session for the Author. After any substantive file edit, run `~/alexandria/system/canon/change-closure.md` before calling the task complete; discover explicit and semantic downstream effects, resolve each one, verify the assembled behavior, and refresh the local receipt without making the Author remember. Keep product corrections local for the Author; never turn them into company feedback. Never replace platform memory or the Author's pre-existing instructions.
<!-- alexandria:end -->
