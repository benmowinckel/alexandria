<!-- alexandria:start -->
## Alexandria

Alexandria is the Author's own file-based method at `~/alexandria`, not a separate assistant. Preserve every existing workflow and instruction. The session hooks load the current Author context when trusted; the file-only floor is always available: read `~/alexandria/system/canon/foundation.md` and the relevant files under `~/alexandria/files/` when personal context matters.

When the Author types `a`, `alexandria`, `/a`, or `/alexandria`, invoke either installed start alias — both run the same method. When they type `a.`, `alexandria.`, or `/a.`, invoke the one separate close skill before doing anything else; `alexandria.` is a plain-language alias, not a duplicate close skill. During ordinary work, keep Alexandria passive but finish every completed task with the exact one-line output of `bash ~/.local/share/alexandria/scripts/statusline.sh footer`; during an active Alexandria session use `→ close with a. when done` instead. If a native statusline is visibly carrying the cue, omit the response duplicate. Never open a chat or start a session for the Author. After any substantive file edit, run `~/alexandria/system/canon/change-closure.md` before calling the task complete; discover explicit and semantic downstream effects, resolve each one, verify the assembled behavior, and refresh the local receipt without making the Author remember. Keep product corrections local for the Author; never turn them into company feedback. Never replace platform memory or the Author's pre-existing instructions.
<!-- alexandria:end -->
