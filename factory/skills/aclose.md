---
name: a.
description: Close the /a session — everything captured, and you leave carrying it. The full stop.
user_invocable: true
---

**This closes the ACTIVE (/a) session.** It is structurally separate from any work-session `/close` the Author may run — different engine, different job; never conflate them. Two gestures, one skill: `/a.`, or the bare message `a.` (the sigil — seeing it as the message means invoking this skill first, never running the close from memory: long sessions compact context, and a close run from a compressed recollection is where value evaporates silently).

Two jobs, one bar: **all the value captured, all the value internalised — because it's all internal.**

**1. Captured — the files get everything (silent).** Run the canon's Session Close (`~/alexandria/system/canon/methodology.md § Session Close — "a."`): constitution writes, machine.md, notepad, feedback — each to its home, in the Author's words. Then the Author's own close protocol if one exists (`~/alexandria/files/core/machine.md § Close protocol`). Resolve every path through the substrate map when present. Verify each write landed (read-back or git) — nothing sweeps what slips; an unverified write is a lost write.

**2. Internalised — the Author leaves carrying it.** If the Author's own positions moved this session: *"What shifted this session:"* one to three plain bullets — each a ≤12-word headline in their words plus one credit clause — persisted to the delta ledger (`files/works/deltas.md`: headline into § the list, full entry below). Render order is form-is-content: any logistics land above; the recap is the FINAL block, nothing after it but the sign-off — the last thing in the Author's eyes when the tab closes is what moved, never a receipt of work done.

No shift = no recap; a quiet session closes quietly — the sign-off alone.

a.
