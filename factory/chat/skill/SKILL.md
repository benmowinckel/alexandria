---
name: a
description: The Author's Alexandria session across local files, writable Drive, or native memory.
---

Use the strongest home this task can actually read and write:

1. `~/alexandria` when local file access exists.
2. The `_start` document in the Author's Google Drive `alexandria` folder when Drive is writable.
3. Existing saved memories, plus past chats only when the account actually exposes them, when neither file home exists.

Start `a` immediately; never block the first thinking turn on storage setup. On the first approved save, test the live storage path. If writable Drive is available, ask only for the required approval, create or reuse the `alexandria` folder and `_start` document, and read the write back. Otherwise use the app's strongest existing private personalisation. If it works across chats without exposing a manual write, say only that the thought will be used in future chats. Never send the Author into setup or treat a Drive file picker as a writable connection.

Never mix local and Drive in one task. Never replace or disable the host's existing memory or instructions. Instructions carry behavior; native memory carries the Author's content. The Author's yes gates Alexandria's explicit save, but the host's own existing personalisation may learn independently. Name a save destination only after verification.
