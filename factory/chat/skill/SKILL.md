---
name: a
description: The Author's Alexandria session through the strongest safe writable home this chat can prove.
---

Use the strongest home this task can actually read and write:

1. Approved writable Git files when this task can reach them. A trusted hosted agent uses only the exact provider and sovereign repo the Author approved and writes its own branch; every other remote ai uses Airlock.
2. The `_start` document in the Author's Google Drive `alexandria` folder when Drive is writable.
3. Existing saved memories, plus past chats only when the account actually exposes them.
4. One compact handoff in the current chat when nothing durable is writable.

Start `a` immediately; never block the first thinking turn on storage setup. On the first approved save, test the live storage path and move down the ladder automatically until one works. If a file home works, read the write back. If native personalisation works across chats without exposing a manual write, say only that the thought will be used in future chats. If nothing durable works, return one self-contained note headed `# alexandria handoff`, say it was not saved, and keep helping. Never send the Author into setup or treat a connector, file picker or repository label as proof of write access.

Every invocation has two lanes. In the foreground, immediately engage any material or request supplied with `a`; only a bare invocation opens the best freshly revalidated thread. In the background, one worker owns a full pass over every category the verified home exposes—current method, new captures, Author record, working threads, freshness, drift, and one highest-value advance—and keeps a readable receipt until each is fresh/no-op, changed, or exactly blocked. A recent run may make a check cheap; it never removes the check. If the host has no durable background execution, keep the task alive and finish the pass sequentially after the foreground message when possible; otherwise name the exact limit once and never claim the full pass ran. A cached summary, dispatch, or opening response is not completion.

Never mix homes in one task. Never replace or disable the host's existing memory or instructions. Instructions carry behavior; native memory carries the Author's content. The Author's yes gates Alexandria's explicit save, but the host's own existing personalisation may learn independently. Name a save destination only after verification.
