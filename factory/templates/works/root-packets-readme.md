# Root packets

One file per position. Switch to a different model family in this same
app and review this exact file. Cursor + Fable then Cursor + Grok is
enough. Same app does not matter.

This is a pause, not a lock. Leave the packet here until that review
and the Author's own-word signoff — or override on purpose.
