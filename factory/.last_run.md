# Factory Run — 2026-05-16

**Status:** Aborted. Could not authenticate to `/admin/factory`.

**Root cause:** `gh` CLI is not installed in the scheduled remote execution environment. `gh auth token` is the sole auth path for the admin endpoint; no GITHUB_TOKEN or equivalent was present in env.

**What was completed:**
- Spec fetched and read in full (`factory/skills/factory.md`)
- Signals: unreachable (auth blocked)
- Checkpoint: not advanced (auth blocked)
- Canon PRs / recent history: not checked

**Fix needed:** Add `gh` CLI to the remote session image, or inject `GITHUB_TOKEN` as an environment variable in the session configuration at https://code.claude.com. The server accepts `Authorization: github <token>` and validates the token against GitHub's `/user` endpoint — a fine-grained PAT scoped to `mowinckelb/alexandria` (read/write on contents + pull requests) is sufficient.

**Next run:** Will process all accumulated signals from the beginning (marker not advanced, so nothing is lost).
