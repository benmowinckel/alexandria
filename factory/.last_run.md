# Factory Run — 2026-05-16 (16:00 UTC)

**Status:** Aborted. Cannot reach `api.alexandria-library.com` — host not in CCR network allowlist.

**Root cause:** Different from the earlier failure today (00:01 UTC, no `gh` CLI / GITHUB_TOKEN). That was fixed. The new blocker is the CCR environment's outbound network policy: `api.alexandria-library.com` is not whitelisted, so all calls to `/admin/factory` and `/admin/factory/checkpoint` are rejected at the network layer before auth is even attempted.

- Bash `curl`: returns "Host not in allowlist"
- WebFetch: returns 403 without forwarding the auth header

The admin API key embedded in the system prompt (`Bearer alex_…`) cannot help when the host is unreachable.

**What was completed:**
- Spec fetched and read in full (`factory/skills/factory.md`) — succeeded via GitHub MCP
- Signals: unreachable (network blocked)
- Checkpoint: not advanced (network blocked)
- Open factory-autoloop PRs: none (confirmed via GitHub search)
- Recent canon history: read — last 20 commits, no pending canon work

**Fix needed:** Add `api.alexandria-library.com` to the CCR session's network allowlist in the routine config at https://code.claude.com. The spec's setup notes already call this out explicitly ("Network allow-list must include `api.alexandria-library.com`").

**Next run:** Marker not advanced — all accumulated signals remain. Will process everything from the beginning on next successful run.
